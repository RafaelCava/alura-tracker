FROM node:16-alpine

WORKDIR /home/node/app

RUN apk add --no-cache \
  bash \
  git \
  curl

COPY package.json .

RUN yarn install

COPY . .

RUN yarn build

ENTRYPOINT [ "yarn", "start" ]

USER node

EXPOSE 8080